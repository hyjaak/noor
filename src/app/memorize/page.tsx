"use client";
import Link from "next/link";
import { useState } from "react";
import { memorizationStatus, updateMemorization, type MemorizationState } from "../../../packages/learning/memorization";
const initial: MemorizationState = { ease: 2.5, interval: 0, repetitions: 0, dueAt: new Date(0) };
export default function MemorizePage() { const [state, setState] = useState(initial); const [revealed, setRevealed] = useState(false); const attempt = (quality: 0 | 3 | 5) => { setState(updateMemorization(state, quality)); setRevealed(false); }; return <main className="page-shell narrow-shell"><div className="section-heading"><div><p className="eyebrow">MEMORIZATION · NON-VOICE</p><h1>Stay with one ayah.</h1><p className="lede">A quiet recall exercise for Al-Fatihah 1:1.</p></div><span className="status-pill">{memorizationStatus(state)}</span></div><section className="memory-card glass-card"><p className="eyebrow">COMPLETE THE AYAH</p><p className="memory-prompt">بِسْمِ اللَّهِ الرَّحْمَنِ <span className={revealed ? "revealed" : "hidden-word"}>{revealed ? "الرَّحِيمِ" : "..."}</span></p><button className="quiet-button" onClick={() => setRevealed(true)}>{revealed ? "Answer shown" : "Show answer"}</button><div className="quality-row"><button onClick={() => attempt(0)}>Needs more time</button><button onClick={() => attempt(3)}>Getting there</button><button onClick={() => attempt(5)}>I know it</button></div><p className="muted">Next review follows a gentle spaced schedule. No raw score is shown.</p></section><section className="memory-options"><article className="disabled-exercise glass-card"><p className="eyebrow">RECITE WITHOUT ASSISTANCE</p><h2>Voice mode unlocked</h2><p className="muted">Record a full recitation for batch, best-effort feedback. Nothing is kept by default.</p><Link className="text-link" href="/recitation">Open recitation coach →</Link></article><article className="disabled-exercise glass-card"><p className="eyebrow">RECALL AFTER DELAY</p><h2>Voice mode unlocked</h2><p className="muted">Use the same privacy-gated coach when you are ready to recall this ayah aloud.</p><Link className="text-link" href="/recitation">Open recitation coach →</Link></article></section></main>; }
