"use client";
import { useState } from "react";
const tools = [{ href: "#tasbih", icon: "◌", name: "Tasbih", detail: "A quiet counter for remembrance" }, { href: "#duas", icon: "د", name: "Du'a library", detail: "Supplications for everyday moments" }, { href: "#adhkar", icon: "✧", name: "Morning & evening adhkar", detail: "Begin and close the day with remembrance" }, { href: "#calendar", icon: "☾", name: "Islamic calendar", detail: "11 Safar 1448 · 25 August 2026" }, { href: "#zakat", icon: "◈", name: "Zakat calculator", detail: "A simple way to begin your estimate" }];
export default function UtilitiesPage() { const [count, setCount] = useState(0); return <main className="page-shell narrow-shell"><p className="eyebrow">YOUR TOOLKIT</p><h1>Useful moments.</h1><p className="lede">Simple spaces for the practices around your salah.</p><div className="utility-grid">{tools.map((tool) => <a className="utility-card glass-card" href={tool.href} key={tool.name}><span className="utility-icon">{tool.icon}</span><div><h2>{tool.name}</h2><p className="muted">{tool.detail}</p></div><span className="arrow">→</span></a>)}</div><section id="tasbih" className="tasbih glass-card"><p className="eyebrow">TASBIH</p><h2>SubhanAllah</h2><button className="tasbih-count" onClick={() => setCount(count + 1)}>{count}</button><p className="muted">Tap to count · your count stays on this device only</p><button className="quiet-button" onClick={() => setCount(0)}>Reset</button></section></main>; }
