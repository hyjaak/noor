"use client";
import Link from "next/link";
import { useState } from "react";
import { createClient } from "../../lib/supabase/client";
export default function AuthPage() { const [mode, setMode] = useState<"signin" | "signup">("signup"); const [message, setMessage] = useState(""); const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const email = String(form.get("email")); const password = String(form.get("password")); const supabase = createClient(); const result = mode === "signup" ? await supabase.auth.signUp({ email, password }) : await supabase.auth.signInWithPassword({ email, password }); if (result.error) setMessage(result.error.message); else window.location.assign(mode === "signup" ? "/onboarding" : "/"); }; return <main className="page-shell auth-page"><div className="auth-mark">ن</div><p className="eyebrow">NOOR · YOUR JOURNEY</p><h1>{mode === "signup" ? "Begin gently." : "Welcome back."}</h1><p className="lede">{mode === "signup" ? "Create a private space for your Quran and salah." : "Continue from wherever you left off."}</p><form className="onboarding-form" onSubmit={submit}><label>Email<input name="email" type="email" required placeholder="you@example.com" /></label><label>Password<input name="password" type="password" required placeholder="At least 8 characters" /></label><button className="primary-button" type="submit">{mode === "signup" ? "Create account →" : "Sign in →"}</button></form>{message && <p className="error-text" role="alert">{message}</p>}<button className="text-link auth-switch" onClick={() => setMode(mode === "signup" ? "signin" : "signup")}>{mode === "signup" ? "Already have an account? Sign in" : "New to Noor? Create an account"}</button><small className="privacy-note">Authentication uses Supabase Auth sessions. Your account data is not stored in browser memory.</small><Link className="quiet-button" href="/onboarding">Continue to onboarding</Link></main>; }
