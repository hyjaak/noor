import { deriveEncounteredVocabulary, foundationEvents } from "../../../../packages/learning";
export default function ArabicPage() { const vocabulary = deriveEncounteredVocabulary(foundationEvents); return <main className="page-shell learning-shell"><p className="eyebrow">ARABIC PATH · FROM YOUR READING</p><h1>Words you&apos;ve met.</h1><p className="lede">A personal vocabulary shelf, drawn from the ayahs you have actually read.</p><section className="arabic-hero glass-card"><p className="eyebrow">ENCOUNTERED IN AL-FATIHAH</p><div className="word-display">رَبِّ</div><p><b>rabb</b> · Lord</p><span className="muted">Read in your Quran journey · tap a word to stay with it</span></section><div className="vocab-grid">{vocabulary.map((item) => <article className="vocab-card glass-card" key={item.word}><span className="vocab-word">{item.word}</span><h2>{item.sound}</h2><p>{item.meaning}</p><small>{item.source}</small></article>)}</div><p className="muted lesson-source">Vocabulary is sourced from your Quran reading history, not a generic course list.</p></main>; }
