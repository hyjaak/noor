"use client";
import { useState } from "react";
export default function OnboardingPage() { const [done, setDone] = useState(false); const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); await fetch("/api/settings", { method: "PUT", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name: form.get("name"), location: form.get("location"), learningLevel: form.get("learningLevel"), madhab: form.get("madhab") }) }); setDone(true); }; return <main className="page-shell onboarding"><p className="eyebrow">WELCOME TO NOOR</p><h1>Let&apos;s make it yours.</h1><p className="lede">A few details help us show the right prayer times and reading pace.</p><form className="onboarding-form" onSubmit={submit}><label>Your name<input name="name" required placeholder="e.g. Noor" /></label><label>City or location<input name="location" required placeholder="e.g. New York, NY" /></label><label>Learning level<select name="learningLevel" defaultValue="adult_beginner"><option value="kids">Kids</option><option value="teen">Teen</option><option value="adult_beginner">Adult beginner</option><option value="adult_intermediate">Adult intermediate</option><option value="advanced">Advanced</option></select></label><label>Madhab<select name="madhab" defaultValue="shafii"><option value="shafii">Shafi&apos;i</option><option>Hanafi</option><option>Maliki</option><option>Hanbali</option></select></label><button className="primary-button" type="submit">{done ? "You&apos;re ready" : "Begin my journey →"}</button></form></main>; }
